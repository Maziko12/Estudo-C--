#include <iostream>
using namespace std;

/*Escreva uma função chamada soma que recebe dois números inteiros como parâmetros e retorna a soma dos dois números.

Escreva uma função chamada ehPar que recebe um número inteiro como parâmetro e retorna true se o número for par e false caso contrário.

Escreva uma função chamada calcularMedia que recebe um vetor de números inteiros como parâmetro e retorna a média dos valores no vetor.

Escreva uma função chamada inverterString que recebe uma string como parâmetro e retorna uma nova string com os caracteres invertidos.

Escreva uma função chamada fatorial que recebe um número inteiro como parâmetro e retorna o fatorial desse número.

Escreva uma função chamada contarVogais que recebe uma string como parâmetro e retorna a quantidade de vogais presentes na string.

Escreva uma função chamada ehPrimo que recebe um número inteiro como parâmetro e retorna true se o número for primo e false caso contrário.

Escreva uma função chamada calcularPotencia que recebe dois números inteiros como parâmetros, base e expoente, e retorna o valor da base elevada ao expoente.

Escreva uma função chamada concatenarStrings que recebe duas strings como parâmetros e retorna uma nova string que contém a concatenação das duas strings.

Escreva uma função chamada encontrarMaior que recebe um vetor de números inteiros como parâmetro e retorna o maior valor presente no vetor.*/

