#include <iostream>
#include <chrono>

namespace first
{
  int numero;
  std::cout << "Me diga um número que eu te mostrarei o FATORIAL dele." << std::endl << "R: " << numero;
  std::cin = numero;
}

int Fatorial(int first::numero)
{
  Fatorar = first::numero * (first::numero - 1)
  return Fatorar
}

int Traves(string & = str)
{
  std::string Tracejados;
  for (int i = 0; i <= 20; i++)
  {
    Tracos = "-_-";
    std::string Tracejados = " " + Tracos;
  }
  return std::string Tracejados;
}

int main()
{
  std::cout << "Seja bem-vindo ao meu programa!" << std::endl;
  std::cout << "Eu acabei de criá-lo, pode ter erros de sintaxe!" << std::endl;
  std::cout << Traves << std::endl;
  std::cout << "Olá, vamos começar o programa!" << std::endl;
  std::this_thread::sleep_for(std::chrono::seconds(1));
  std::cout << first::numero << std::endl;
  std::this_thread::sleep_for(std::chrono::seconds(1));
  std::cout << Traves << std::endl;
  int Fator = Fatorial(first::numero);
  std::cout << "O valor é " << Fator << "!" << std::endl;
  std::cout << Traves << std::endl;
  std::cout << "Este é o programa! Obrigado por testá-lo!" << std::endl;
  std::cout << Traves << "Fim" << Traves;
  return 0;
}